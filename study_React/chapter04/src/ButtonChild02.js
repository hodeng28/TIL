import React, { Component } from 'react';
import PropTypes from 'prop-types';

const ButtonChild02 = ({ title, color }) => {
  const ButtonStyle = {
    border: "none",
    width: 100,
    height: 30,

  }
}